{
  "name": "json-space-analyzer",
  "scripts": {
    "dev": "bun x concurrently -n server,bun \" bun x lite-server --baseDir public\" \"bun run build --watch --sourcemap=external\"",
    "build": "bun build src/index.ts --minify --outdir public",
    "format": "bun x prettier --write ."
  },
  "dependencies": {
    "sunburst-chart": "^1.19.0"
  }
}
